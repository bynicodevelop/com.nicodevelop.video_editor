import { AudioWavePlayerDirective } from './audio-wave-player.directive';

describe('AudioWavePlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioWavePlayerDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
